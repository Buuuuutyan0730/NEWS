# FX（ドル円）情報収集・分析ツール 開発計画書

## 1. ツール概要

ドル円（USD/JPY）に関する経済イベント・金融ニュース・X投稿を自動収集し、
Gemini AIで「円安/円高」方向と影響度（1〜5）を判定、Webブラウザ上で一覧表示するツール。

---

## 2. 技術スタック

| 項目 | 採用技術 | 理由 |
|------|----------|------|
| 言語 | Python 3.10+ | ユーザー環境に導入済み |
| Webフレームワーク | Flask | 軽量・シンプル、個人利用に最適 |
| フロントエンド | HTML + CSS + JavaScript（Bootstrap 5） | レスポンシブ対応でPC/スマホ両対応 |
| データベース | SQLite | インストール不要、ファイル1つで完結 |
| AI分析 | Google Gemini API（gemini-2.0-flash） | 無料枠あり、日本語分析精度が十分 |
| タスクスケジューラ | APScheduler | Python内蔵で1分間隔の自動取得を実現 |

---

## 3. 情報ソースと取得方法

### 3-1. 経済イベントカレンダー

| 優先度 | ソース | 取得方法 | 備考 |
|--------|--------|----------|------|
| 第1候補 | **Investing.com** | Webスクレイピング（requests + BeautifulSoup） | 経済指標カレンダーページから重要度付きで取得 |
| 第2候補（フォールバック） | **FinnHub API** | REST API（無料枠） | Investing.comが取得不可の場合の代替 |

- 取得対象：USD・JPY関連の経済指標（雇用統計、CPI、FOMC、日銀会合等）
- 取得タイミング：毎日0時に当日分を一括取得 + 1分ごとに差分チェック

### 3-2. 金融ニュース

| ソース | 取得方法 | 備考 |
|--------|----------|------|
| **Yahoo Finance（日本語）** | RSSフィード解析 | 為替・経済ニュースカテゴリ |
| **Reuters（日本語）** | RSSフィード解析 | 外国為替ニュース |
| **日経（電子版）** | RSSフィード解析 | マーケットニュース |

- RSSフィードは `feedparser` ライブラリで安定取得
- キーワードフィルタ：「ドル円」「為替」「円安」「円高」「FRB」「日銀」「金利」等

### 3-3. X（旧Twitter）投稿

**対象アカウント：**
- `@financialjuice`（英語・速報ニュース）
- `@yahoojpfinance`（日本語・Yahoo!ファイナンス）

**取得方法（優先順位）：**

| 優先度 | 方法 | 説明 |
|--------|------|------|
| 第1候補 | **RSSHub** | オープンソースのRSS生成サービス。公開インスタンスまたはローカルで動作。X投稿をRSSフィード化して取得 |
| 第2候補 | **Nitter RSS** | Nitter公開インスタンス経由でRSS取得（インスタンスの生存状況に依存） |
| 第3候補 | **手動RSSフィード登録** | 上記が不安定な場合、ユーザーがRSSフィードURLを設定ファイルに登録する方式 |

> **補足：X API有料プランについて**
> - Basic Plan: 月額$200（月10,000件読み取り） → 個人利用にはやや高額
> - 上記の無料手段で安定性に問題がある場合、有料プランへの切り替えも設定で対応可能な設計にする

---

## 4. AI分析（影響度判定）の仕組み

### 4-1. 使用API
- **Google Gemini API**（`gemini-2.0-flash` モデル）
- 無料枠：1分あたり15リクエスト、1日1,500リクエスト → 本ツールの用途には十分

### 4-2. 分析プロンプト設計

各ニュース/投稿に対し、以下の構造化プロンプトを送信：

```
あなたはFX（ドル円）の専門アナリストです。
以下のニュースを読み、ドル円相場への影響を分析してください。

【ニュース本文】
{ニュースの内容}

【回答形式（JSON）】
{
  "direction": "円安" or "円高" or "中立",
  "impact": 1〜5の整数（1=ほぼ影響なし, 5=極めて大きい影響）,
  "reason": "判定理由（50文字以内）"
}
```

### 4-3. 分析の実行タイミング
- 新しいニュース/投稿を取得するたびに即座に分析
- 過去に分析済みの記事は再分析しない（DBで管理）

---

## 5. データベース設計（SQLite）

### テーブル構成

```sql
-- ニュース・投稿テーブル
CREATE TABLE news_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    source TEXT NOT NULL,          -- 'investing', 'rss_yahoo', 'x_financialjuice' 等
    title TEXT NOT NULL,
    content TEXT,
    url TEXT,
    published_at DATETIME,
    fetched_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    direction TEXT,                -- '円安', '円高', '中立'
    impact INTEGER,               -- 1〜5
    ai_reason TEXT,               -- AI判定理由
    analyzed INTEGER DEFAULT 0    -- 分析済みフラグ
);

-- 日次サマリーテーブル（カレンダー用）
CREATE TABLE daily_summary (
    date TEXT PRIMARY KEY,         -- 'YYYY-MM-DD'
    total_items INTEGER,
    yen_weak_count INTEGER,        -- 円安要因の件数
    yen_strong_count INTEGER,      -- 円高要因の件数
    avg_impact REAL,
    summary TEXT,                  -- AIによるその日の要約
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 6. 画面設計

### 6-1. メイン画面（ダッシュボード）

```
┌─────────────────────────────────────────────────┐
│  FX ドル円 分析ダッシュボード     [手動更新] [設定]  │
├─────────────────────────────────────────────────┤
│                                                   │
│  ◆ 総合メーター（ゲージ表示）                        │
│  ┌───────────────────────────────┐               │
│  │   ← 円高  ●━━━━━━━●━━━  円安 →│               │
│  │         現在の総合傾向: やや円安               │
│  └───────────────────────────────┘               │
│                                                   │
│  ◆ 本日の経済イベント                               │
│  ┌──────┬──────────┬─────┬──────┬──────┐        │
│  │ 時刻  │ イベント名   │ 重要度 │ 方向  │ 影響度 │        │
│  ├──────┼──────────┼─────┼──────┼──────┤        │
│  │ 21:30 │ 米雇用統計   │ ★★★  │ 円安↗ │ ████▒ │        │
│  │ 08:50 │ 日銀短観     │ ★★   │ 円高↘ │ ██▒▒▒ │        │
│  └──────┴──────────┴─────┴──────┴──────┘        │
│                                                   │
│  ◆ 最新ニュース＆X投稿                              │
│  ┌──────┬────┬──────────┬──────┬──────┐         │
│  │ 時刻  │ 情報源│ 見出し     │ 方向  │ 影響度 │         │
│  ├──────┼────┼──────────┼──────┼──────┤         │
│  │ 14:32 │ X   │ 米CPI予想... │ 円安↗ │ ███▒▒ │         │
│  │ 14:15 │ RSS │ 日銀総裁...  │ 円高↘ │ ████▒ │         │
│  └──────┴────┴──────────┴──────┴──────┘         │
│                                                   │
│  最終更新: 2026-04-06 14:33:00  次回更新: 14:34:00  │
└─────────────────────────────────────────────────┘
```

### 6-2. カレンダー画面（履歴閲覧）

- 月間カレンダー形式で表示
- 各日付セルに「円安/円高マーク」と件数を表示
- 日付クリック → その日のニュース一覧＋AI要約を表示
- 過去1年分のデータを保持

### 6-3. レスポンシブ対応
- Bootstrap 5のグリッドシステムで PC/スマホ自動切替
- スマホ表示時はカード形式にレイアウト変更

---

## 7. ファイル構成

```
NEWS/
├── app.py                  # メインアプリケーション（Flask）
├── config.py               # 設定ファイル（APIキー、取得間隔等）
├── requirements.txt        # 依存パッケージ一覧
├── PLAN.md                 # 本計画書
│
├── collectors/             # 情報収集モジュール
│   ├── __init__.py
│   ├── economic_calendar.py  # 経済イベント取得
│   ├── rss_news.py           # RSSニュース取得
│   └── x_fetcher.py          # X投稿取得
│
├── analyzer/               # AI分析モジュール
│   ├── __init__.py
│   └── gemini_analyzer.py    # Gemini APIによる影響度分析
│
├── database/               # データベース関連
│   ├── __init__.py
│   ├── models.py             # テーブル定義・初期化
│   └── fx_data.db            # SQLiteデータベースファイル（自動生成）
│
├── templates/              # HTMLテンプレート
│   ├── base.html             # 共通レイアウト
│   ├── dashboard.html        # メインダッシュボード
│   └── calendar.html         # カレンダー履歴画面
│
└── static/                 # 静的ファイル
    ├── css/
    │   └── style.css          # カスタムCSS
    └── js/
        └── dashboard.js       # ダッシュボード動的処理
```

---

## 8. 開発ステップ（実行順序）

### Step 1: プロジェクト基盤構築
- [ ] ファイル構成の作成
- [ ] `requirements.txt` の作成
- [ ] `config.py`（設定ファイル）の作成
- [ ] SQLiteデータベースの初期化コード
- [ ] Flaskアプリの最小構成（Hello Worldレベル）で起動確認

### Step 2: 経済イベントカレンダー取得機能
- [ ] Investing.comからの経済イベント取得スクレイピング実装
- [ ] フォールバック（代替API）の実装
- [ ] 取得データのDB保存処理
- [ ] 動作確認（取得→保存→表示）

### Step 3: RSSニュース取得機能
- [ ] Yahoo Finance / Reuters / 日経 のRSSフィード取得実装
- [ ] FX関連キーワードフィルタリング
- [ ] 取得データのDB保存処理
- [ ] 動作確認

### Step 4: X（旧Twitter）投稿取得機能
- [ ] RSSHub経由でのX投稿取得実装
- [ ] フォールバック（Nitter等）の実装
- [ ] 取得データのDB保存処理
- [ ] 動作確認

### Step 5: Gemini AI分析機能
- [ ] Gemini API連携の実装
- [ ] 分析プロンプトの実装・チューニング
- [ ] 分析結果のDB保存処理
- [ ] 動作確認（サンプルニュースで判定精度チェック）

### Step 6: メインダッシュボード画面
- [ ] 共通HTMLレイアウト（Bootstrap 5）
- [ ] 総合メーター（円安←→円高ゲージ）
- [ ] 経済イベント一覧テーブル
- [ ] ニュース＆X投稿一覧テーブル
- [ ] 影響度のビジュアル表示（カラーバー）
- [ ] 手動更新ボタン
- [ ] 1分間隔の自動更新（JavaScript）
- [ ] レスポンシブ対応（スマホ表示確認）

### Step 7: カレンダー履歴画面
- [ ] 月間カレンダー表示
- [ ] 日付クリック → その日の詳細表示
- [ ] 日次AIサマリー生成（1日の終わりに自動実行）
- [ ] 過去データの自動削除（1年超のデータ）

### Step 8: 自動スケジューリング・仕上げ
- [ ] APSchedulerによる1分間隔の自動取得設定
- [ ] 日次バッチ処理（サマリー生成・古いデータ削除）
- [ ] エラーハンドリング・ログ出力
- [ ] 最終動作確認・使い方メモ

---

## 9. 設定項目（config.py）

| 設定名 | 説明 | デフォルト値 |
|--------|------|-------------|
| GEMINI_API_KEY | Gemini APIキー | （ユーザーが設定） |
| FETCH_INTERVAL_MINUTES | 自動取得間隔（分） | 1 |
| X_ACCOUNTS | 監視するXアカウント | @financialjuice, @yahoojpfinance |
| RSSHUB_BASE_URL | RSSHubのベースURL | https://rsshub.app |
| DB_PATH | データベースファイルパス | database/fx_data.db |
| HISTORY_RETENTION_DAYS | 履歴保持日数 | 365 |
| KEYWORDS | FX関連フィルタキーワード | ドル円, 為替, 円安, 円高, ... |

---

## 10. 起動方法（完成後）

```bash
# 1. 依存パッケージのインストール（初回のみ）
pip install -r requirements.txt

# 2. config.py にGemini APIキーを設定

# 3. アプリ起動
python app.py

# 4. ブラウザでアクセス
# http://localhost:5000
```

---

## 11. 注意事項・リスク

| リスク | 対策 |
|--------|------|
| Investing.comのスクレイピングがブロックされる可能性 | FinnHub APIをフォールバックとして実装 |
| RSSHub公開インスタンスの不安定性 | 複数インスタンスのフォールバック + 手動RSS登録機能 |
| Gemini API無料枠の上限到達 | リクエスト数カウント＋上限接近時の警告表示 |
| X投稿の取得が完全に不可能になる場合 | 設定画面でX機能のON/OFF切替を用意 |
